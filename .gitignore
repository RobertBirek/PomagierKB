# środowisko / sekrety
.env
*.env
!*.env.example
!.env.example
secrets/
*.pem
*.key
!**/test/fixtures/**/*.key

# node
node_modules/
dist/
coverage/
*.tsbuildinfo

# dane runtime (żyją w /srv/kag-data, nie w repo)
/data/
*.db
*.db-wal
*.db-shm

# raporty sędziego LLM (tools/eval/judge.mjs) — zawierają pytania użytkowników;
# domyślnie lądują w $DATA_DIR/exports, wpis chroni przed ręcznym JUDGE_OUT w repo
tools/eval/judge-report-*.json

# różne
.DS_Store
*.log
.remember/
